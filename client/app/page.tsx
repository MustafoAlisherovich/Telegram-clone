import { Button } from '@/components/ui/button'

const HomePage = () => {
	return (
		<div className=''>
			<Button variant={'destructive'} size={'lg'}>
				Hello next 16
			</Button>
		</div>
	)
}

export default HomePage
